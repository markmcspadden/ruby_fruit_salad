<!DOCTYPE html>
<html>
<head>
  <title>Ruby Fruit Salad</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <header>
    <h1>ruby fruit salad</h1>
  </header>

  <nav>
    <h3>
      <% if @exercise && @exercise[:id].to_i != 1 %>
        <%= link_to "< Previous", exercise_path(:id => @exercise[:id].to_i-1) %>
      <% end %>
      <% if @exercise && @exercise[:id].to_i != 1 && @exercise[:id].to_i != Exercise.count %>
        &nbsp;&nbsp;|&nbsp;&nbsp;
      <% end %>
      <% if @exercise && @exercise[:id].to_i != Exercise.count %>
        <%= link_to "Next >", exercise_path(:id => @exercise[:id].to_i+1) %>
      <% end %>
    </h3>
  </nav>

  <%= yield %>

  <footer>
    Built with love by <a href="http://github.com/markmcspadden">@markmcspadden</a> on #whyday 2013<br/>
    <a href="https://github.com/markmcspadden/ruby_fruit_salad">Get the Source</a>
  </footer>
</body>
</html>
